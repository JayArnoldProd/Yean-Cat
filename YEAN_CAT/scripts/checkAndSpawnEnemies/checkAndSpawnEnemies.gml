// Check and spawn enemies//this will be used later.
function checkAndSpawnEnemies(bpm) {
    var nextSpawn = nextSpawnTime(bpm);
    if (currentSyncedTime() >= nextSpawn) {
        // Implement spawn logic
    }
}